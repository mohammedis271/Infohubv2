<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.muaazjoosuf.infohubv2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NewsSearchActivity"
            android:theme="@style/Theme.AppCompat.Light" />
        <activity
            android:name=".ArticleViewActivity"
            android:label="@string/title_activity_article_view"
            android:theme="@style/Theme.AppCompat.Light" />
        <activity
            android:name=".NewsSectionActivity"
            android:theme="@style/Theme.AppCompat.Light" />
        <activity android:name=".SplashScreenLoadingActivity" />
        <activity
            android:name=".OfflineActivity"
            android:theme="@style/Theme.AppCompat.Light" />
        <activity
            android:name=".MoviesSectionActivity"
            android:theme="@style/Theme.AppCompat.Light" />
        <activity
            android:name=".SelectedMovieActivity"
            android:theme="@style/Theme.AppCompat.Light" />
        <activity
            android:name=".SelectedMovieCastsPage"
            android:theme="@style/Theme.AppCompat.Light" />
        <activity
            android:name=".SelectedMovieTrailerPage"
            android:theme="@style/Theme.AppCompat.Light" />
        <activity
            android:name=".SelectedMovieReviewPage"
            android:theme="@style/Theme.AppCompat.Light" />
    </application>

</manifest>